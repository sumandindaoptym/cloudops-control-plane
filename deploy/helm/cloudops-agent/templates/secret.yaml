{{/*
Secret template is intentionally empty.
Users must create the secret manually before deploying:

kubectl create secret generic cloudops-agent-secret \
  --from-literal=API_KEY=your-api-key-here \
  -n <namespace>

Or specify an existing secret via .Values.existingSecret
*/}}
